services:
  - type: web
    name: studypilot_backend
    env: python
    runtime: python-3.12
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2

